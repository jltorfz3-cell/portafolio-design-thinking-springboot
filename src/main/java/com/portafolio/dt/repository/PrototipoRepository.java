package com.portafolio.dt.repository;
import com.portafolio.dt.model.Prototipo; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*;
public interface PrototipoRepository extends JpaRepository<Prototipo,Long>{  }
