package com.portafolio.dt.repository;
import com.portafolio.dt.model.DefinicionProblema; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*;
public interface DefinicionProblemaRepository extends JpaRepository<DefinicionProblema,Long>{  }
