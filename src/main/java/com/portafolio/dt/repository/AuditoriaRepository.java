package com.portafolio.dt.repository;
import com.portafolio.dt.model.Auditoria; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*;
public interface AuditoriaRepository extends JpaRepository<Auditoria,Long>{  }
